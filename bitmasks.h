#ifndef BITMASK_H
    #define BITMASK_H

    #define ORIGINAL_BITMASK    0x80 // Use to mark an original number
    #define ROW_SELECT          0x40 // Use to mark selected row
    #define COL_SELECT          0x20 // Use to mark selected col
    #define NUMBER_MASK         0x0F // Use to isolate a cell's number
#endif